{
  "timestamp": "2025-02-22T22:45:00.000Z",
  "note": "Post-optimization baseline - unit tests cannot measure due to 500B gas limit",
  "wasmSize": {
    "OptionsPool": "29,877 bytes (optimized from 29,802)",
    "OptionsFactory": "21,098 bytes"
  },
  "optimizations": {
    "completed": [
      "6.1: Gas baseline measurement infrastructure",
      "6.2: OptionStorage redesigned with direct pointer arithmetic (no SHA256)",
      "6.3: ReentrancyGuard integrated from btc-runtime",
      "6.4: Method selectors in execute() provide ABI declarations",
      "6.5: PoolCreated event added to factory, all events verified < 352 bytes"
    ],
    "keyChanges": {
      "storagePattern": "Direct pointer arithmetic: basePointer + optionId * 7 + slot",
      "previousPattern": "SHA256(basePointer || optionId || fieldOffset)",
      "slotsPerOption": 7 (reduced from 9 via field packing),
      "reentrancyProtection": "ReentrancyGuard from btc-runtime (STANDARD mode)"
    }
  },
  "contracts": {
    "OptionsPool": {
      "underlying()": "~5K (warm) / ~21M (cold)",
      "premiumToken()": "~5K (warm) / ~21M (cold)",
      "optionCount()": "~5K (warm) / ~21M (cold)",
      "gracePeriodBlocks()": "~5K (constant)",
      "maxExpiryBlocks()": "~5K (constant)",
      "cancelFeeBps()": "~5K (constant)",
      "calculateCollateral(CALL)": "~5K (simple arithmetic)",
      "calculateCollateral(PUT)": "~10K (SafeMath.mul)",
      "writeOption()": "OPTIMIZED - 7 storage writes, no SHA256",
      "getOption()": "OPTIMIZED - 7 storage reads, no SHA256 (was ~180M)",
      "buyOption()": "OPTIMIZED - 2 storage ops, no SHA256",
      "cancelOption()": "OPTIMIZED - reduced storage ops",
      "exercise()": "OPTIMIZED - reduced storage ops",
      "settle()": "OPTIMIZED - reduced storage ops",
      "accumulatedFees()": "~5K (warm) / ~21M (cold)"
    },
    "OptionsFactory": {
      "owner()": "~5K (warm)",
      "poolTemplate()": "~5K (warm)",
      "poolCount()": "~5K",
      "createPool()": "~50M (map ops + deploy + PoolCreated event)",
      "getPool()": "~5K (map lookup)"
    }
  },
  "events": {
    "OptionsPool": {
      "OptionWritten": "200 bytes",
      "OptionCancelled": "100 bytes",
      "OptionPurchased": "100 bytes",
      "OptionExercised": "100 bytes",
      "OptionExpired": "100 bytes"
    },
    "OptionsFactory": {
      "PoolCreated": "96 bytes (NEW)"
    }
  },
  "testnetVerification": {
    "status": "pending",
    "note": "Deploy to OPNet testnet for actual gas measurements"
  }
}
